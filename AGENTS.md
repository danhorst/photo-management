# photo-management

Project goals and critical behavior live in `openspec/project.md` — read it first for context.
OpenSpec specs in `openspec/specs/` are the current truth; proposals in `openspec/changes/`.
